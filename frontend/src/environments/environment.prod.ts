export const environment: { production: boolean; apiUrl: string } = {
  production: true,
  // Replace with your backend public URL before deploy (example: https://alpay-backend.onrender.com/api)
  apiUrl: 'https://YOUR_BACKEND_URL/api'
};
